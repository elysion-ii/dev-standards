# {{APP_NAME}} — Agent Instructions

{{APP_NAME}} is a {{APP_TYPE_LABEL}} application targeting `{{TFM}}`.

`CLAUDE.md` at the repository root is a one-line import of this file. This file is the
repository's router — facts, commands, and reading instructions; it holds no rule text.
Rule bodies live under `docs/rules/`. Edit this file, never `CLAUDE.md`.

## Technology Stack

| Item | Detail |
|------|--------|
| Language | C# |
| Runtime | {{TFM}} |
| UI | {{APP_UI_DESCRIPTION}} |
| Distribution | Self-contained single EXE + Inno Setup installer |

## Applications

| Application | Projects | Rules | Specification |
|---|---|---|---|
| {{APP_NAME}} | {{APP_NAME}}, {{APP_NAME}}.Tests | `docs/rules/{{APP_NAME}}.md` | `docs/specs/{{APP_NAME}}.md` |

## Rules and AUDIT

- **Before implementing any change**, read in order: `docs/rules/standard.md` (shared core), `docs/rules/dotnet.md` (.NET rules), then the application's rules file and specification from the Applications table. On conflict the more specific file wins (application > language > core)
- When a change requires behavior not in the specification, update the specification **before** implementing (spec-first — see the Specifications section of `docs/rules/standard.md`)
- **When transitioning from a plan to implementation**, re-read this file (root and any nested `AGENTS.md` covering the work area) and the rules files first, so all rules are loaded before code is written
- **Before reporting an implementation task as complete**, run the AUDIT procedure at the end of `docs/rules/standard.md`
- **Before running any `gh pr merge`**, read the `merge-pr` skill (distributed in this repository at `.claude/skills/` and `.agents/skills/`)
- `docs/rules/standard.md` and `docs/rules/dotnet.md` are managed by dev-standards — never edit them; repository- and application-specific rules go in `docs/rules/{{APP_NAME}}.md`

## Commands

| Purpose | Command |
|---------|---------|
| Format | `dotnet format {{APP_NAME}}.slnx` |
| Format check (must pass before completion) | `dotnet format {{APP_NAME}}.slnx --verify-no-changes` |
| Build | `dotnet build {{APP_NAME}}/{{APP_NAME}}.csproj -c Release` |
| Test | `dotnet test {{APP_NAME}}.Tests/{{APP_NAME}}.Tests.csproj` |
| Full build (format gate → tests → publish) | `build/Menu.bat` (interactive) or `powershell -ExecutionPolicy Bypass -File build/Build.ps1` |
| Installer | `powershell -ExecutionPolicy Bypass -File build/Installer.ps1` |

## Directory Layout

### `{{APP_NAME}}/`

The main project. Contains all application source code.

### `{{APP_NAME}}.Tests/`

xUnit test project.

### `build/`

Build scripts and installer configuration.

- `Build.ps1` runs format verification (`dotnet format --verify-no-changes`) and tests, then publishes {{APP_NAME}} as a self-contained single-file EXE to `build/{{APP_NAME}}/`. Both gates must pass before publish proceeds
- `Installer.ps1` invokes Inno Setup (ISCC.exe) on `Setup_{{APP_NAME}}.iss`; requires `build/{{APP_NAME}}/{{APP_NAME}}.exe` to exist. Before running ISCC it reads `<Version>` from `Directory.Build.props`, injects it via `/DMyAppVersion`, and — if `CHANGELOG.md` exists — verifies it contains a heading for the current version (fails otherwise). The version and CHANGELOG rules are `docs/rules/dotnet.md` VERSION
- Output directories: `build/{{APP_NAME}}/` (self-contained EXE) and `build/Installer/` (installer package), produced only by the scripts and gitignored (rules: `docs/rules/dotnet.md` OUTPUT)

### `docs/`

All non-source documents, placed in role-based subfolders (`rules/`, `adr/`, `specs/`,
`guides/`, `references/`, `investigations/`, `notes/`, `plans/`, `inbox/`, `archive/`).
Before creating, moving, renaming, or archiving any document — or when unsure where one
belongs — read the `doc-placement` skill (also distributed in this repository) first; it
defines placement, naming, and front matter. Do not classify or name documents from
memory.

- `docs/rules/` — rule bodies: `standard.md` + `dotnet.md` (managed by dev-standards) and `{{APP_NAME}}.md` (application rules)
- `docs/specs/{{APP_NAME}}.md` — the {{APP_NAME}} specification: what it does
- `docs/adr/` — Architecture Decision Records; retired ADRs move to `docs/adr/archive/`
- `docs/plans/` and `docs/archive/plans/` — working area for plans, gitignored: plans never enter the repository, in progress or archived

