---
status: active
created: {{DATE}}
---

# {{APP_NAME}} Rules

Application-specific rules for {{APP_NAME}}. Write only deltas and overrides against the
shared rules (`standard.md`, `dotnet.md` in this directory) — never copy shared rule
text here. On conflict, this file wins.

## Repository Language

- `AGENTS.md`, this file, and all other agent instruction files in this repository are written in **English only**
- Keep rules concise and declarative. Do NOT include concrete code examples unless absolutely necessary — reference the relevant source file/method instead

## Application Rules

(No application-specific rules yet. Add them here as they emerge — with rationale in an ADR when the reasoning matters.)
