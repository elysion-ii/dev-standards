; {{APP_NAME}} Installer Script
; MyAppVersion is injected by Installer.ps1 (/D) from Directory.Build.props <Version>.
; Never hardcode a version here — Directory.Build.props is the single source of truth.
#ifndef MyAppVersion
  #error MyAppVersion is not defined. Build via Installer.ps1, which injects it from Directory.Build.props.
#endif
#define MyAppName "{{APP_NAME}}"
#define MyAppExeName "{{APP_NAME}}.exe"

[Setup]
AppId={{{{APP_ID_GUID}}}
AppName={#MyAppName}
AppVersion={#MyAppVersion}
DefaultDirName={autopf}\{#MyAppName}
DisableProgramGroupPage=yes
OutputDir=Installer
OutputBaseFilename={#MyAppName}-Setup-{#MyAppVersion}
Compression=lzma
SolidCompression=yes
ArchitecturesAllowed=x64compatible
ArchitecturesInstallIn64BitMode=x64compatible
PrivilegesRequired=lowest

[Languages]
Name: "japanese"; MessagesFile: "compiler:Languages\Japanese.isl"

[Tasks]
Name: "desktopicon"; Description: "デスクトップにショートカットを作成"; Flags: unchecked
Name: "startmenu"; Description: "スタートメニューにショートカットを作成"

[Files]
; Ship the whole publish output, not just the executable: a native dependency
; adds files beside it (see docs/rules/dotnet.md, NATIVEDEP).
Source: "{{APP_NAME}}\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs
; An application with a configuration file excludes it from the line above
; (Excludes: "appsettings.json") and installs it with onlyifdoesntexist, so an
; update keeps the operator's settings (see docs/rules/dotnet.md, CONFIGFILE).

[Icons]
Name: "{userdesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; Tasks: desktopicon
Name: "{userprograms}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; Tasks: startmenu

[Run]
Filename: "{app}\{#MyAppExeName}"; Description: "{#MyAppName} を起動"; Flags: nowait postinstall skipifsilent
