---
status: active
created: {{DATE}}
---

# {{APP_NAME}} 仕様書

{{APP_NAME}} が何をするかを書く。振る舞いのみ — クラス構成・利用ライブラリ・内部アルゴリズムは
書かない。spec-first: ここに書かれていない振る舞いが必要な変更は、**実装前に**本書を変更する
(`docs/rules/standard.md` の Specifications 節を参照)。

## 目的

({{APP_NAME}} が存在する理由 — 解決する問題。)

## 適用範囲

(本仕様書が扱う範囲と、アプリケーションを構成するプロジェクト。)

## 利用者と外部システム

(誰が使うか。どの外部システムと通信するか。)

## 必須の振る舞い

({{APP_NAME}} が示すべき振る舞い。必要に応じて要求IDを付ける:)

<!--
### {{APP_NAME}}-001: <要求タイトル>

<トリガー>の場合、<必須の振る舞い>。
-->

## 入力と出力

## エラー時の振る舞い

## 不変条件

## 非機能要件

## 対象外

## 要求とテストの対応

(要求IDを使う場合、各要求をどのテストが検証するかを記載する。)
